# Free key from https://openrouteservice.org/dev/#/signup (Dashboard > Request a token > Directions)
VITE_ORS_API_KEY=

# Server-only (no VITE_ prefix — never bundled into client code). Used by
# api/generate-shape.js for the "...or describe anything" AI shape generator.
# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
